<%= form_for (@bootcamp_review), url: bootcamp_review_path(@bootcamp) do |f| %>

  <% if @bootcamp_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bootcamp_review.errors.count, "error") %> prohibited this bootcamp from being saved:</h2>

      <ul>
      <% @bootcamp_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <div id="star-reviewing"></div>
    <div class="form-group">
        <%= f.label :comment %>
        <%= f.text_field :body %>
    </div>
    <%= f.submit 'add Comment' %>
<% end %>